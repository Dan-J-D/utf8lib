# ut8flib
A Simple Pure C99 utf8 encoder and decoder.

### Build with CMake For Release X64:
```bash
cmake -S src -B build
cmake --build build
```

### Build with CMakeAuto
```bash
cmakeauto build
```
